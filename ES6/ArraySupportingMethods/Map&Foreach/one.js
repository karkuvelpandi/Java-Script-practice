
let array = [10, 20, 30, 40, 50]

array.map(() => {
    console.log("hello man");
})

//map iterates the array and exicute our function for array length times

array.map((e) => {
    console.log(e);    //map itrerates the array and excicute the function to give elements inside the array 
});                    // (function can use any name to iterate)  

