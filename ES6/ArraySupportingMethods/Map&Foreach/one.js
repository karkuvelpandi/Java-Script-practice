
let array = [10, 20, 30, 40, 50]
                             //map iterates the array and exicute our function for array length time
array.map((arr) => {
    console.log(arr);    //map itrerates the array and excicute the function to give elements inside the array 
});                       // (function can use any name to iterate)  

